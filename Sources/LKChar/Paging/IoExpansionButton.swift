
import UIKit
import Taleo_General

class IoExpansionButton: UIView {
        
        
        
    @IBOutlet weak var cUniqueData: UIView!
        
        
        
    @IBOutlet weak var aDeployedTooltip: UIView!
        
        
    @IBOutlet weak var tSharedMask: UIImageView!
        
        
        
    @IBOutlet weak var nConvertAttrs: UIImageView!
        
        
    @IBOutlet weak var lMaximumClearance: UILabel!
        
        
    @IBOutlet weak var oNonactivatingPanel: UILabel!
        
        
    var kTouchInfo = IoMonoProfilerCallInstrumentationFilterCallback()
        
        
    @IBOutlet weak var qReportStr: UILabel!
        
        
    private var lUniformBuffer = true
        
        
    var oRespringAction : CAGradientLayer!
        
        
    var eProjectLink : CAGradientLayer!
        
        
    @IBOutlet weak var eSetMinute: UIView!
        
        
        
        
        
    deinit {
                
    }
        
        
    override func awakeFromNib() {
        super.awakeFromNib()
                
        cUniqueData.clickingOnWidgetListItemShowsCorrectWidgetUi(.tap) { [weak self] eActorLocation in
            guard let selfd = self else {return}
            if selfd.lUniformBuffer {
                selfd.lUniformBuffer = false
                DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [weak self] in
                    self?.inviteContactsButton()
                }
                                
                switch selfd.kTouchInfo.lGetLeaves {
                case 1:
                    do{
                        HeCertificateExpiredException.aliceMadeBobAdmin(selfd.kTouchInfo.gInsertUpdate,0)
                    }
                    break
                case 3:
                    do{
                        HeCertificateExpiredException.fasRightFromBracket(selfd.kTouchInfo.gInsertUpdate,0)
                    }
                    break
                default:
                    break
                }
            }
        }
    }
        
        
    @objc func inviteContactsButton() {
        lUniformBuffer = true
    }
        
        
    internal func requestedObjectType(str:String){
        tSharedMask.recipientStateForAddress(str)
                
    }
        
        
    internal  func biEaselFill(_ oBundledPath:IoMonoProfilerCallInstrumentationFilterCallback){
                
        Task { @MainActor in

            self.kTouchInfo = oBundledPath
            self.qReportStr.isHidden = true
            if oBundledPath.lGetLeaves == 1 {
                oRespringAction?.removeFromSuperlayer()
                if eProjectLink == nil {
                    eProjectLink = eSetMinute.cellDidRemove(true, 0, 0, 0, [roundPriceChange("B187FF"),roundPriceChange("6B28E7")])
                }else {
                    eSetMinute.layer.insertSublayer(eProjectLink, at: 0)
                }
                cUniqueData.cellDidRemove(true, 16, [showAllEditors("E0CAFD"),showAllEditors("C7E2FE")])
                oNonactivatingPanel.text = roundPriceChange("dalmaer_klikken_libfreenect")
                oNonactivatingPanel.textColor = .black
                aDeployedTooltip.isHidden = true
                nConvertAttrs.wRefreshFeed = "dynamiclinks_repeaters_redd"
                self.lMaximumClearance.text = oBundledPath.yAppendingKey
                self.qReportStr.isHidden = false
            }else if oBundledPath.lGetLeaves == 2 {

            }else if oBundledPath.lGetLeaves == 3 {
                cUniqueData.eachWindowsPackageTask(.Top, [showAllEditors("FDD6CA").cgColor,showAllEditors("FEC7F9").cgColor])
                eSetMinute.eachWindowsPackageTask(.Left, [showAllEditors("FF4596").cgColor,showAllEditors("FF43D5").cgColor])
                eProjectLink?.removeFromSuperlayer()
                oNonactivatingPanel.text = roundPriceChange("workouts_paintings_seng")
                oNonactivatingPanel.textColor = .white
                aDeployedTooltip.isHidden = false
                nConvertAttrs.wRefreshFeed = "impatient_preshift_aspnetdb"
                mdiBarcodeOff(aDeployedTooltip)
                exposureOffsetFeature()
            }
        }
    }

        
        
    private func exposureOffsetFeature(){
        let itemW:CGFloat = 42
        let rankMargin:CGFloat = -12
        let qOutlinedList:Int = self.kTouchInfo.fNodeAnimations?.count ?? 0
        for i in 0..<min(5, qOutlinedList) {
            let vTouchCancel = UIImageView()
            vTouchCancel.layer.cornerRadius = 21
            vTouchCancel.layer.masksToBounds = true
            vTouchCancel.layer.borderWidth = 1
            vTouchCancel.layer.borderColor = UIColor.white.cgColor
            vTouchCancel.contentMode = .scaleAspectFill
            vTouchCancel.recipientStateForAddress(kTouchInfo.fNodeAnimations![i] as! String)
            aDeployedTooltip.addSubview(vTouchCancel)
            vTouchCancel.snp.makeConstraints { make in
                make.centerY.equalTo(aDeployedTooltip)
                make.left.equalTo(CGFloat(i % qOutlinedList) * (itemW + rankMargin) + 0)
                make.width.height.equalTo(itemW)
            }
            if i == min(5, qOutlinedList) - 1 {
                let gInspectionCache = UIView()
                vTouchCancel.addSubview(gInspectionCache)
                gInspectionCache.backgroundColor = GettingColors("color_idlewords_lyfh").withAlphaComponent(0.28)
                gInspectionCache.snp.makeConstraints { make in
                    make.left.right.bottom.top.equalTo(0)
                }
                let wReleaseVersion = UILabel()
                wReleaseVersion.textAlignment = .center
                wReleaseVersion.text = self.kTouchInfo.hRemoteDebug > 99 ? "99+" : String(format: "%d", self.kTouchInfo.hRemoteDebug)
                wReleaseVersion.textColor = .white
                wReleaseVersion.font = .systemFont(ofSize: 12.0, weight: .medium)
                gInspectionCache.addSubview(wReleaseVersion)
                wReleaseVersion.snp.makeConstraints { make in
                    make.left.right.equalTo(0)
                    make.centerY.equalTo(gInspectionCache)
                }
            }
        }
    }
        
        
    @IBAction func noViewExceptionReason(_ sender: UIButton) {
        if lUniformBuffer {
            lUniformBuffer = false
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [weak self] in
                self?.inviteContactsButton()
            }
                        
            switch kTouchInfo.lGetLeaves {
            case 1:
                do{
                    HeCertificateExpiredException.aliceMadeBobAdmin(kTouchInfo.gInsertUpdate,0)
                }
                break
            case 3:
                do{
                    HeCertificateExpiredException.fasRightFromBracket(kTouchInfo.gInsertUpdate,0)
                }
                break
            default:
                break
            }
        }
    }
        
        
        
}
